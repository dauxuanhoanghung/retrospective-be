type Query {
  serverInfo: ServerInfo!
}

type ServerInfo {
  host: String! @upper
  databaseUri: String!
  databaseUser: String!
  databasePassword: String!
  environment: String!
  frontendUrl: String! @lower
}
